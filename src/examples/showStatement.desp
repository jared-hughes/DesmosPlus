show y==x
show y==2*x, @{color: GREEN, lineOpacity: 1}
show (5,0)
let a = (5,0)
show a, @{color: rgb(255,186,100), pointSize: 6}
show polygon((0,0),(0,1),(1,1),(1,0))
show midpoint((10,5),a)
show y < x-30
show 2 < x < 3
show f(y) == x
show y == f(x)
show [(-4,0),(-5,0)],
  @{
    color: RED,
    pointOpacity: 0.8,
    onClick: [
      $a <- (8,0),
      $b <- 3
    ]
  }
